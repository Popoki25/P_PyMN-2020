#include <stdio.h>
int _numero, a, b, tmp;
int main(int argc, char const *argv[]) {
  printf(""- Serie de Fibonacci -"");
  print("Por favor ingrese la cantidad de números de la serie que se imprimirán:" );
  scanf("%d\n",&_numero );

    b = 1

    for (size_t i = 1; i < _numero; i++) {
      printf("%d\n",a );
      tmp = a + b
      a = b
      b = tmp
    }
  return 0;
}
